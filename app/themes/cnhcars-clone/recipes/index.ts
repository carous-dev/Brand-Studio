// Empty recipe registry — cnhcars-clone is a faithful page-for-page clone of
// the source app and doesn't use the dashboard's section/recipe system. The
// contract requires this file to exist; an empty registry is the explicit
// "no recipes" signal.
import type { ThemeRecipeRegistry } from '../../types'

export const themeRecipes: ThemeRecipeRegistry = {}

export default themeRecipes
