import type { ThemePageProps } from '../../../types'
import CompareClient from './CompareClient'

export function AxisComparePage(_: ThemePageProps) {
  return <CompareClient />
}

export default AxisComparePage
