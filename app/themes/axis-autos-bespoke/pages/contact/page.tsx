import type { ThemePageProps } from '../../../types'
import ContactClient from './ContactClient'

export function AxisContactPage(_: ThemePageProps) {
  return <ContactClient />
}

export default AxisContactPage
