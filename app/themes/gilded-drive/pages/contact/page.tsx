import type { ThemePageProps } from '../../../types'
import ContactUs from '../../components/ContactUs'
import '../../styles/contact.css'

export function GildedContactPage(_: ThemePageProps) {
  return (
    <main>
      <ContactUs />
    </main>
  )
}

export default GildedContactPage
