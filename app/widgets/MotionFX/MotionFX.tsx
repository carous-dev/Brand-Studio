import './styles.css'

/**
 * MotionFX — server-component CSS injector.
 *
 * Mount once in any theme's Shell (alongside <AnimateOnScroll />). The
 * companion `styles.css` registers the neon/motion @keyframes + utility
 * classes used across all themes:
 *   .mfx-glow-pulse     — radial brand-tinted glow, breathes infinitely
 *   .mfx-glow-orbit     — orbiting glow blob (sweeps in a circle)
 *   .mfx-shimmer        — animated linear gradient sweep (button hover, badges)
 *   .mfx-pulse-dot      — status-indicator pulse (live stock, in-store now)
 *   .mfx-float          — gentle vertical float loop (hero badges, decorations)
 *   .mfx-tilt           — subtle rotation loop (decorative chips)
 *   .mfx-scan           — vertical scan-line sweep (futuristic overlay)
 *   .mfx-text-glow      — text-shadow loop (hero highlights, key numbers)
 *   .mfx-border-glow    — box-shadow loop (CTA buttons, featured cards)
 *   .mfx-spotlight      — radial mask following cursor (uses CSS @property)
 *   .mfx-grid-drift     — slowly-shifting grid background
 *
 * All animations honour `prefers-reduced-motion: reduce`.
 */
export default function MotionFX() {
  return null
}
